{
  "name": "counter-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.0.0",
    "dotenv": "^16.0.3",
    "env-cmd": "^10.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite-plugin-env-compatible": "^1.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^2.1.0",
    "vite": "^3.1.0"
  },
  "scripts": {
    "phoenix": "rm -f yarn.lock && rm -rf ./node_modules && yarn --no-fund --no-audit",
    "start": "npx http-server ./dist",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "develop": "env-cmd -e develop npm run dev"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
